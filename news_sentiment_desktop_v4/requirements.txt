# News Sentiment Desktop V4 (Claude Edition) - Python requirements
# Recommended Python: 3.10 - 3.12
# NOTE: keep this file ASCII-only. pip decodes it with the Windows locale
# codepage (cp950 on Traditional Chinese systems) and non-ASCII comments
# break `pip install -r`.

# ---- GUI ----
PySide6>=6.6,<7.0

# ---- Anthropic official SDK ----
anthropic>=0.40.0

# ---- OpenAI official SDK (provider switch, V4.3.0) ----
openai>=2.0.0

# ---- Data import ----
openpyxl>=3.1.2

# ---- Article scraping ----
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=5.1.0
# Use the OS certificate store for SSL (fixes corporate proxy/firewall TLS issues)
truststore>=0.8.0

# ---- Article scraping: browser rendering fallback (optional feature) ----
# After install, also run once: playwright install chromium
# (run_desktop.bat does this automatically on first launch)
playwright>=1.44.0
gne>=0.3.0

# ---- Word export ----
python-docx>=1.1.0

# ---- Secure API key storage (Windows Credential Manager / DPAPI) ----
keyring>=24.3.0

# ---- Gmail news import (OAuth, read-only scope) ----
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# ---- Packaging to exe (future) ----
pyinstaller>=6.3.0

# ---- Tests ----
pytest>=8.0.0
pytest-mock>=3.12.0
